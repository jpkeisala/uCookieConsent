@model Fluid.Plugins.uCookieConsent.CookieInfo

@if (Model != null && Model.EnableCookieBar)
{
    if (Model.ThrdPartyTurn)
    {
        @Html.Raw(Model.ThrdPartyFooterSection)
    }


    if (Model.AdditionalTurn)
    {
        @Html.Raw(Model.AdditionalFooterSection)
    }
}
